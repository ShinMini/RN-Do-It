/** @format */

import React, {Component} from 'react';
import {StyleSheet, Text, View, TouchableOpacity} from 'react-native';
import CameraScreenNode from './src/screen/CameraScreenNode';

const App = () => {
  return (
    <View>
      <CameraScreenNode></CameraScreenNode>
    </View>
  );
};

export default App;
